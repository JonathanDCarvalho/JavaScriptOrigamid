// Linha de Comando (CLI)

// ls - lista o diretório
// ls -all - lista tudo do diretório, incluindo arquivos ocultos

// cd + nome = acessa um diretório
// cd .. - volta um diretório

// mkdir + name = cria uma pasta
// rm -r + name = remove a pasta

// touch + arquivo (script.js) = cria um arquivo
// rm + arquivo = remove um arquivo